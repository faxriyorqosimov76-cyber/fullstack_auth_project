server {
    listen 80;
    
    location /api {
        proxy_pass http://86.106.181.36:4000;
    }

    location / {
        root /var/www/html;
        index index.html;
        try_files $uri /index.html;
    }
}